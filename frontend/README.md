# Frontend (React + Vite)

This is a minimal React frontend scaffold for the Task Analysis backend.

Defaults
- Backend base URL: `http://localhost:5000/api` (override with `VITE_API_URL`)

Quick start

```bash
cd frontend
npm install
npm run dev
```

Notes
- Backend must be running for auth / data requests to succeed.
